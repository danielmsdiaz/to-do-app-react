import axios from "axios";

export const postTask = async (task: string) => {
  //console.log(process.env.REACT_APP_BASE_URL);
  
  // try {
  //   const resposta = await axios.post(process.env.REACT_APP_BASE_URL as string, {
  //     firstName: 'Fred',
  //     lastName: 'Flintstone'
  //   });
  // } catch (erro: any) {
  //   throw new erro;
  // }
}